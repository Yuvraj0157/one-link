<!-- Header -->
<%-include('include/_header')%>
<!-- /Header -->
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">

 <!-- Main Site -->
 <div class="container mt-3">
    <div class="row">
    <div class="col-md-12">
      <div class="jumbotron text-center">
        <h2>Please Verify your email</h2>
        <p>You're almost there! We've sent an email to: <br> <b> <%=user.email%> </b> </p>
        <br>
        <p>Just click on the link in that email to complete your signup.<br>If you don't see it, you may need to <b>check your spam</b> folder.</p>
        <p>Still can't find the email?</p>
        <!-- <button type="button" class="btn btn-success">Resend Email</button>
        <p id="resend-timer"></p>
        <br> -->
        <p>Contact us at <em>projtodoapp@gmail.com</em> </p>
      </div>
    </div>
  </div>
</div>
<!-- /Main Site -->

<!-- include footer -->
<%-include('include/_footer')%>
<!-- /include footer -->

<!-- <script>
  // Resend Email
  document.querySelector('.btn-success').addEventListener('click', function(){
    fetch('/resend-email', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json'
      },
      body: JSON.stringify({
        email: "<%=user.email%>"
      })
    });

    document.querySelector('.btn-success').style.display = 'none';
    document.getElementById("resend-timer").style.display = 'block';
    
    let timeleft = 60;
    let downloadTimer = setInterval(function(){
      if(timeleft < 0){
        clearInterval(downloadTimer);
        document.querySelector('.btn-success').style.display = 'inline-block';
        document.getElementById("resend-timer").style.display = 'none';
      } else {
        document.getElementById("resend-timer").innerHTML = "Resend Email in " + timeleft + " seconds";
      }
      timeleft -= 1;
    }, 1000);
  });
</script> -->
